# Set to true to run the extension locally without Google OAuth.
# When true, auth will use a mock token/profile for UI development.
# Note: Drive API calls will fail when using the mock token (MOCK_ACCESS_TOKEN).
# To enable real Drive sync, set this to false and add your client ID into manifest.json.
VITE_USE_MOCK_AUTH=true